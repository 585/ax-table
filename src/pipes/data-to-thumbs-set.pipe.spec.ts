import { DataToThumbsSetPipe } from './data-to-thumbs-set.pipe';

describe('DataToThumbsSetPipe', () => {
  it('create an instance', () => {
    const pipe = new DataToThumbsSetPipe();
    expect(pipe).toBeTruthy();
  });
});
